<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Upload New CV</title>
    <script src="https://cdn.tailwindcss.com"></script>
     <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;700&display=swap" rel="stylesheet">
     <style>
        body { font-family: 'Inter', sans-serif; }
    </style>
</head>
<body class="bg-gray-100 flex flex-col items-center justify-center min-h-screen">

    <div class="bg-white p-8 rounded-lg shadow-md w-full max-w-md">
        <h1 class="text-2xl font-bold text-center text-gray-800 mb-6">Upload New CV</h1>

         <% if (typeof message !== 'undefined' && message) { %>
            <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative mb-4" role="alert">
                <span class="block sm:inline"><%= message %></span>
            </div>
        <% } %>

        <p class="text-gray-600 text-center mb-4">Select a new PDF file for your CV. This will replace the current <code class="bg-gray-200 px-1 rounded text-sm">cv.pdf</code> file.</p>

        <form action="/upload" method="POST" enctype="multipart/form-data">
            <div class="mb-4">
                <label for="cvFile" class="block text-gray-700 text-sm font-bold mb-2">Choose PDF File:</label>
                <input type="file" id="cvFile" name="cvFile" accept="application/pdf" required
                       class="shadow appearance-none border rounded w-full py-2 px-3 text-gray-700 leading-tight focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent file:mr-4 file:py-2 file:px-4 file:rounded file:border-0 file:text-sm file:font-semibold file:bg-blue-50 file:text-blue-700 hover:file:bg-blue-100">
                 <p class="text-xs text-gray-500 mt-1">Max file size: 5MB. Only PDF format allowed.</p>
            </div>
            <div class="flex items-center justify-between">
                <button type="submit"
                        class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded focus:outline-none focus:shadow-outline w-full">
                    Upload CV
                </button>
            </div>
        </form>
    </div>

     <div class="mt-6 text-center">
         <form action="/logout" method="POST">
            <button type="submit" class="text-sm text-red-500 hover:text-red-700 underline">Log Out</button>
         </form>
     </div>
     <div class="mt-2 text-center">
         <a href="/" class="text-sm text-blue-500 hover:text-blue-700">Back to Portfolio</a>
     </div>

</body>
</html>
